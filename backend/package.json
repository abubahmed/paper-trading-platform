{
  "name": "paper-trading-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "uv run uvicorn main:app --reload",
    "flush": "uv run python -m scripts.flush_db --confirm",
    "seed": "uv run python -m scripts.seed_db",
    "seed:fresh": "uv run python -m scripts.seed_db --flush",
    "test-alpaca": "uv run python -m scripts.test_alpaca",
    "redis:start": "docker start redis",
    "redis:stop": "docker stop redis",
    "redis:ping": "docker exec -it redis redis-cli ping"
  },
  "author": "",
  "license": "ISC"
}
